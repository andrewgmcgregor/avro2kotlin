show it working with a kstream in the demo
generate kotlin from schema files as well

make the smoke test 3 real tests
support enums (needed to be useful for cleanser)
support arrays
support maps
support other innerTypes

one kotlin file per protocol
recursive innerTypes
support multiple non-null innerTypes in a union (prob convert to Any/Any?)
serde, hiding the avro type

make the output classes easy to read and understand: hide converter methods in a factory

standalone converter tool: configure which directory you send it to
